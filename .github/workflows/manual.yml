on:
  repository_dispatch:
        types: [incident_report]
        options:
        - staging
        - production
        
jobs:
 handle-incident:
    runs-on: ubuntu-latest
    steps:
      - name: print incident details
        run: echo "Incident reported"
